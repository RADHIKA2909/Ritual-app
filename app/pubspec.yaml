name: app
description: "Ritual — Build habits together. (Flutter web app)"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  dio: ^5.9.2
  firebase_core: ^3.13.1
  firebase_auth: ^5.5.2
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  go_router: ^17.2.3
  shared_preferences: ^2.5.5
  socket_io_client: ^3.1.4
  intl: ^0.20.2
  file_picker: ^8.1.7
  confetti: ^0.7.0
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
